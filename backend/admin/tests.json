{
  "tests": [
    {
      "name": "Admin access denied without token",
      "method": "GET",
      "path": "/stats",
      "expectedStatus": 403,
      "expectedBody": {"error": "Доступ запрещён"},
      "bodyMatcher": "partial"
    }
  ]
}
